0) Новый документ - новая сессия (NewDocument, OpenDocument)? (Done) (CloseDocument?)
1) Учитывать начало и конец команд (Done)
2) Есть ли смысл учитывать одинаковые команды подряд? А одинаковые команды в одной последовательности?
3) Нужно ли строго придерживаться порядка логов, если команды выполнились в одну и ту же секунду? Думаю нужно (Done)
4) Убрать Exit из последовательностей (Inspector, StartupVPerfTest, TipOfDay) (Done)
5) Нужно ли учитвать <>, которые внутри других команд?
*) Может попробовать отсеивать последовательности по лифту а не по поддержке?
*) Попробовать задавать max_support?


' (При min_gap < 0 могут возникнуть ситуции при который последовательность команд нарушается)

Интерфейс:
1) Добавить вывод таблиц в отдельном окне (Done)
2) Запускать считывание логов и алгоритм GSP в отдельном потоке, отображая прогресс
3) Добавить выбор игнорируемых команд в интерфейс
*) Записывать результат алгоритма в файл

Программа:
1) Попробовать делать сразу insert нескольких строк, а не по одной
*) Сделать считывание базы данных по частям, а не целиком